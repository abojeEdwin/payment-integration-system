<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Project Coordinates -->
    <groupId>com.paybridge</groupId>
    <artifactId>payment-integration-system</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>PayBridge</name>
    <description>Production-grade payment integration platform with multi-PSP support</description>
    <url>https://github.com/ifeanyicyriacus/payment-integration-system</url>

    <!-- Organization -->
    <organization>
        <name>Ifeanyi Cyriacus</name>
        <url>https://github.com/ifeanyicyriacus</url>
    </organization>


    <!-- Contact -->
    <developers>
        <developer>
            <id>ifeanyicyriacus</id>
            <name>Ifeanyi Cyriacus</name>
            <email>ifeanyicyriacus(at)gmail.com</email>
            <roles>
                <role>Architect</role>
                <role>Developer</role>
            </roles>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/ifeanyicyriacus/payment-integration-system.git</connection>
        <developerConnection>scm:git:https://github.com/ifeanyicyriacus/payment-integration-system.git</developerConnection>
        <url>https://github.com/ifeanyicyriacus/payment-integration-system</url>
        <tag>HEAD</tag>
    </scm>

    <!-- Module Declaration -->
    <modules>
        <module>common</module>
        <module>auth-service</module>
        <module>payment-service</module>
        <module>webhook-service</module>
    </modules>

    <!-- Properties (Centralized Versions) -->
    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Spring Boot -->
        <spring-boot.version>3.5.10</spring-boot.version>

        <!-- Dependencies -->
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <postgresql.version>42.7.3</postgresql.version>
        <kafka.version>3.8.0</kafka.version>
        <mapstruct.version>1.6.2</mapstruct.version>
        <lombok.version>1.18.34</lombok.version>
        <jjwt.version>0.12.5</jjwt.version>
        <jacoco.version>0.8.10</jacoco.version>

        <!-- Testing -->
        <testcontainers.version>1.20.1</testcontainers.version>
        <awaitility.version>4.2.1</awaitility.version>
    </properties>

    <!-- Dependency Management (BOM Style) -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Build Plugins (Centralized) -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- No dependencies in parent - only dependencyManagement -->
</project>
